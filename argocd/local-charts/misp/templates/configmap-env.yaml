apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "misp.fullname" . }}-env
  labels:
    {{- include "misp.labels" . | nindent 4 }}
data:
{{- range $key, $val := .Values.env }}
{{- if $val }}
  {{ $key }}: {{ $val | quote }}
{{- end }}
{{- end }}