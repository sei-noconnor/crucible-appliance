apiVersion: v1
kind: Secret
metadata:
  name: {{ include "crucible.fullname" . }}-credentials
  labels:
    {{- include "crucible.labels" . | nindent 4 }}
type: Opaque
data:
  postgres-password: {{ .Values.credentials.postgresPassword | b64enc | quote }}
