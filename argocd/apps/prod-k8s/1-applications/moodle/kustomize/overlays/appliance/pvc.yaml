apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: moodle-script-pvc
  namespace: default
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  resources: 
    requests:
      storage: 1Gi
  
